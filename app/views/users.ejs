<!DOCTYPE html>
<html>
	<head>
		<title>Users</title>
	</head>
	<body>
		Sciao belo
		<br>
		<% for(i=0; i < users.length; i++) { %>
			<a href="/ui/users/<%= users[i]._id %>/profile"><%= users[i].name %></a>
			<br>
		<% } %>
	</body>
</html>
